import React from 'react';
import css_clases from './Settings.module.css';

const Settings = (props) => {
  return(
    <div className={css_clases.content}>
      Hello!
    </div>
  );
};

export default Settings;